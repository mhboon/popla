// Cognito's default password policy (not customized — see
// ARCHITECTURE.md's Auth section): min. 8 characters, upper/lowercase, a
// number, a symbol.
export const PASSWORD_HINT =
  'At least 8 characters, with uppercase, lowercase, a number and a symbol.';
